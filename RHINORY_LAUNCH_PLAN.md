# RHINORY 실제 오픈 준비 문서

현재 저장소는 소비자 사이트와 운영자 백오피스를 함께 가진 Next.js 앱입니다. 외부 키가 없는 상태에서도 데모 카탈로그로 화면을 확인할 수 있고, 운영 키를 넣으면 DB·이미지 업로드·문의 저장을 활성화하는 구조입니다.

## 1. 지금 구현된 운영 범위

- 소비자 사이트: 메인, 카테고리, 상품 상세, 프로젝트, 가이드, 상담, 입점, 장바구니, 마이페이지 기본 화면
- 관리자: `/admin`
- 관리자 상품 등록/수정/공개 상태 변경
- 관리자 재고·예약 수량·재주문 기준 관리
- 관리자 문의 조회
- 썸네일 URL 관리와 Vercel Blob 직접 업로드
- 상품/재고/카테고리/문의/주문을 위한 Postgres 스키마
- DB가 없을 때의 데모 모드

## 2. 반드시 발급하거나 결정해야 하는 값

`.env.example`을 복사해 `.env.local`을 만든 뒤 아래 값을 채웁니다.

| 값 | 용도 | 오픈 전 필요 여부 |
| --- | --- | --- |
| `DATABASE_URL` | Neon 또는 호환 Postgres 연결 | 필수 |
| `ADMIN_ACCESS_KEY` | 운영자 백오피스 접근 키 | 필수 |
| `BLOB_READ_WRITE_TOKEN` | 상품/가이드 이미지 업로드 | 권장 |
| `NEXT_PUBLIC_SITE_URL` | canonical URL, 링크, 이메일 URL | 필수 |
| `AUTH_SECRET` | 고객 로그인 세션을 추가할 때 사용 | 회원 기능 오픈 시 필수 |
| `KAKAO_CLIENT_ID`, `KAKAO_CLIENT_SECRET` | 카카오 로그인 | 카카오 로그인을 쓸 때 |
| `RESEND_API_KEY` | 상담·주문·입점 알림 메일 | 운영 알림에 권장 |
| `TOSS_CLIENT_KEY`, `TOSS_SECRET_KEY` | 실제 결제 승인/취소 | 유료 결제 오픈 시 필수 |

키를 코드에 직접 넣지 않습니다. Vercel 프로젝트의 Development/Preview/Production 환경 변수에 각각 등록합니다.

## 3. Vercel + DB 연결 순서

1. Vercel에서 이 저장소를 Import합니다.
2. Vercel Production 환경에 `DATABASE_URL`, `ADMIN_ACCESS_KEY`, `NEXT_PUBLIC_SITE_URL`을 먼저 등록합니다.
3. Neon에서 빈 Postgres 데이터베이스를 만들고 연결 문자열을 `DATABASE_URL`에 넣습니다.
4. 로컬 또는 일회성 배포 환경에서 `npm run db:push`를 실행해 테이블을 생성합니다.
5. 배포 후 `/admin`에 접속해 `demo-admin`이 아닌 운영 `ADMIN_ACCESS_KEY`로 로그인합니다.
6. 관리자에서 카테고리/상품/썸네일/재고를 등록하고 상품 상태를 `PUBLISHED`로 변경합니다.
7. 테스트 상품으로 상담 문의, 재고 저장, 상품 상세, 장바구니 흐름을 검증합니다.

운영 DB가 연결되면 데모 데이터는 자동으로 운영 데이터로 대체되지 않습니다. 관리자에서 seed를 1회 실행하거나 상품을 직접 등록해야 합니다.

## 4. 오픈 전 사업 운영 결정

### 상품 운영

- 상품 타입을 `BUY`, `BUY_INSTALL`, `PROJECT` 세 가지로 유지합니다.
- `BUY`: 가격·옵션·배송비가 명확한 일반 구매 상품
- `BUY_INSTALL`: 제품 가격과 설치 조건을 함께 보여주는 상품
- `PROJECT`: 현장 조건을 받아 견적을 만드는 상품
- 모든 상품에는 SKU, 공급사, 가격 표기, 설치 가능 지역, A/S 주체, 썸네일을 등록합니다.

### 재고 운영

- 직배송 상품은 RHINORY 재고와 공급사 재고를 구분합니다.
- 설치 상품은 재고 대신 `상담 가능`, `제작 기간`, `설치 가능 지역`을 핵심 정보로 관리합니다.
- `quantity - reserved <= reorderPoint`를 재고 경고 기준으로 사용합니다.
- 실제 주문 결제 연동 전까지는 예약 수량을 자동 차감하지 않으므로, 주문 API를 붙일 때 트랜잭션으로 처리해야 합니다.

### 문의·설치·A/S

- 상담 접수 후 담당자, 연락일, 상태, 견적 금액, 공급사 전달 여부를 별도로 기록해야 합니다.
- 설치 상품은 지역, 바닥 조건, 길이/높이, 전기 필요 여부, 사진 첨부를 상담 폼의 필수 데이터로 확장합니다.
- 상품 상세에 배송·설치·A/S 책임 주체를 반드시 공개합니다.

### 법적·신뢰 영역

오픈 전 운영자가 직접 확정해야 합니다.

- 사업자 정보와 통신판매업 정보
- 이용약관, 개인정보처리방침, 구매안전/환불·교환 정책
- 상품별 배송비·설치비·추가 공사비 표기 기준
- 공급사 계약, 정산 주기, 하자·A/S 책임 범위
- 이미지와 설치 사례의 사용 권한

## 5. 결제와 회원 기능의 적용 시점

처음부터 회원가입을 강제하지 않습니다. 초기 전환은 비회원 상담·견적 접수와 장바구니를 먼저 안정화하는 편이 좋습니다.

1. 1단계: 상품 탐색, 상담/입점 문의, 관리자 운영
2. 2단계: 주문서와 Toss 결제, 주문 상태, 이메일 알림
3. 3단계: 카카오 로그인, 주문 조회, 찜/최근 본 상품
4. 4단계: 공급사 포털, 정산, 설치 배정, A/S 티켓

## 6. 배포 전 체크리스트

- [ ] `ADMIN_ACCESS_KEY` 등록 후 운영 로그인 확인
- [ ] Production DB 연결 및 migration/push 완료
- [ ] 관리자 seed 또는 실제 상품 10개 이상 등록
- [ ] 모든 상품 썸네일이 플레이스홀더가 아닌 승인 이미지인지 확인
- [ ] 상품별 가격/설치/배송/A/S 정보 확인
- [ ] 상담 폼이 DB에 저장되고 담당자 알림이 오는지 확인
- [ ] 결제 테스트 키로 성공/실패/취소 시나리오 확인
- [ ] 개인정보·약관·환불 페이지 연결
- [ ] 커스텀 도메인, HTTPS, 검색엔진 canonical 확인
- [ ] 모바일에서 헤더·카테고리·상품 상세·문의 폼 확인
- [ ] Vercel 로그와 DB 백업 정책 확인

## 7. 지금의 안전장치

운영 환경에서 `ADMIN_ACCESS_KEY`가 비어 있으면 관리자는 로그인할 수 없습니다. `demo-admin`은 개발 환경에서만 동작하는 임시 키입니다. 실제 Vercel Production 배포 전에 반드시 운영 키를 등록해야 합니다.
